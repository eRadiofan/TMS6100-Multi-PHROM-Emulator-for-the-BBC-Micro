# Scripts

phromDecode.py is a command-line tool that takes a PHROM binary image and generates structured C code output.

Example:

`python3 phromDecode.py PHROMA.bin >ROMdata_Acorn.c`

The script recognises the Acorn format, the TI-99 format, TI indexed format and the non-indexed format.
